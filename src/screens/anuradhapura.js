import * as React from "react";
import {
  TouchableOpacity,
  View,
  Text,
  SafeAreaView,
  StyleSheet,
  ScrollView,
  Image,
  Dimensions,
} from "react-native";

const anuradhapura = ({ navigation}) => {
    return (
        <SafeAreaView style={{ flex: 1 }}>
            <Text>anuradhapuraaaa</Text>
        </SafeAreaView>
    );
};

export default anuradhapura;