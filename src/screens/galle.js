import * as React from "react";
import {
    TouchableOpacity,
    View,
    Text,
    SafeAreaView,
    StyleSheet,
    ScrollView,
    Image,
    Dimensions,
} from "react-native";

const galle = ({ navigation }) => {
    return (
        <SafeAreaView style={{ flex: 1 }}>
            <Text>galleaaa</Text>
        </SafeAreaView>
    );
};

export default galle;